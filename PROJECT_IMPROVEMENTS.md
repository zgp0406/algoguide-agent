# AlgoGuide Agent 项目改进建议

本文档从整个项目角度整理当前可以改进的方向，不只包括上下文能力，也覆盖产品体验、RAG 检索、知识库管理、后端稳定性、前端交互、测试覆盖、部署演示和简历答辩表达。

## 项目当前判断

AlgoGuide Agent 已经不是一个空壳聊天 Demo。它已经具备这些基础能力：

- FastAPI 后端接口
- 静态前端聊天工作台
- OpenAI 兼容模型调用
- 流式输出
- 本地知识库检索
- 来源引用和证据片段
- 知识库上传、预览、确认入库
- PDF / DOCX / PPTX / Markdown / LaTeX / TXT 解析
- OCR 兜底逻辑
- SQLite 会话保存
- 长会话摘要
- 基础测试用例
- 答辩 PPT 素材

下一步的重点不是“再堆功能”，而是把它打磨成一个更稳定、更可信、更好演示的算法学习助手。

## P0：最值得马上改的部分

### 1. 统一模型输入和上下文结构

当前后端已经会拼接系统提示词、历史对话、摘要和知识库片段，但上下文结构还可以更清晰。

建议：

- 把 prompt 组装集中到一个独立函数或模块
- 固定区分“当前问题 / 会话摘要 / 最近对话 / 知识库片段 / 回答要求”
- 给每次回答返回本轮实际使用的上下文信息
- 当知识库命中质量低时，明确标记“依据不足”

收益：

- 回答更稳定
- 更容易调试 RAG 效果
- 演示时能讲清楚系统不是简单把文本塞给模型

相关文档：

- `CONTEXT_IMPROVEMENTS.md`

### 2. 强化系统提示词

`agent/prompt.py` 目前提示词比较短，适合 MVP，但对算法学习场景的约束还不够细。

建议增加：

- 优先回答当前问题
- 不要把旧上下文强行带入新题
- 知识库无依据时不要假装引用
- 算法题按“思路、状态/步骤、边界、复杂度、代码”组织
- 用户要求简单解释时减少术语
- 用户要求进阶解释时补推导和反例

收益：

- 输出风格更一致
- 算法教学味道更明显
- 简历项目的“Agent 角色设计”更好讲

### 3. 给 RAG 检索加置信度判断（已完成）

当前 `agent/retriever.py` 会返回 Top K 片段，但没有明显的阈值判断。这样可能出现“虽然命中了，但其实不相关”的情况。

建议：

- 给 FAISS 分数和关键词分数分别设置可解释阈值
- 低置信命中时不强行使用 RAG
- 返回 `rag_confidence`、`retrieval_mode`、`low_confidence_reason`
- 前端显示“已使用知识库 / 知识库依据不足”

收益：

- 减少错误引用
- 提升可信度
- 方便答辩时解释“如何避免乱引用”

### 4. 补齐端到端测试

当前 `tests/test_core.py` 已经覆盖了会话存储、文档解析、知识库管理的一部分，但聊天接口和流式接口还可以补。

建议增加：

- `/api/health`、`/api/status` 测试
- `/api/chat` 在无 API Key 时的兜底测试
- `/api/chat/stream` 的流式格式测试
- 知识库上传、预览、确认入库完整流程测试
- 会话重命名、删除、读取历史测试

收益：

- 后续改动更安心
- 项目工程可信度更高
- 面试时能展示不只是“能跑”，而是“可维护”

## P1：增强产品完成度

### 5. 优化知识库管理体验

项目已经有知识库管理功能，下一步可以让它更像真正的学习资料管理器。

建议：

- 支持按知识库筛选聊天检索范围
- 上传后展示解析质量、OCR 状态、切块数量和警告
- 文档详情里展示 chunk 列表
- 支持重新构建某个知识库索引
- 支持禁用某个文档参与检索
- 删除知识库前显示影响范围

收益：

- 用户能控制“让 AI 参考哪些资料”
- RAG 结果更容易解释
- 前端功能更完整

### 6. 改进 PDF / OCR 处理反馈

`KNOWN_ISSUES.md` 已经记录 PDF 乱码问题，`knowledge/library.py` 里也有文本质量检测和 OCR 兜底。下一步重点是让用户知道发生了什么。

建议：

- 上传预览中明确显示解析模式：文本提取 / OCR / 失败
- 显示文本质量分数或简化为“较好 / 一般 / 较差”
- OCR 失败时给出具体解决方式
- 对扫描版 PDF 给出“建议转清晰 PDF 或图片 OCR”的提示

收益：

- 用户不会误以为系统坏了
- PDF 解析问题在演示时有合理解释
- 已知问题可以转化成工程亮点

### 7. 会话能力从“保存记录”升级为“学习进度”（已完成）

现在 SQLite 能保存会话，也能生成摘要。下一步可以围绕算法学习做更明确的学习状态。

建议记录：

- 当前学习主题
- 已掌握概念
- 未解决问题
- 用户偏好的讲解方式
- 最近推荐练习

前端可以显示：

- 当前会话主题
- 最近学习点
- 下一步建议

收益：

- 项目更贴近“学习助手”定位
- 连续对话体验更强
- 与普通聊天壳拉开差距

### 8. 前端交互做成更明确的工作台

当前前端已经有聊天、会话、知识库管理、来源面板。下一步可以优化信息层级和操作反馈。

建议：

- 聊天区固定显示当前知识库范围
- 回答卡片下方展示“来源 / 上下文 / 调试”折叠区
- 生成中允许停止输出
- 输入框支持重试上一问
- 错误提示区分 API 错误、网络错误、解析错误、检索为空
- 移动端重点检查侧栏、抽屉和长文本溢出

收益：

- 使用体验更像完整产品
- 演示过程更稳
- 用户能理解系统当前状态

## P2：工程化和演示加分项

### 9. 增加运行诊断页面

建议增加一个简单的诊断接口或页面，用来检查项目依赖状态。

可以显示：

- API Key 是否配置
- 当前模型名
- embedding 模型是否可用
- FAISS 索引是否存在
- 知识库文档数量
- OCR 依赖是否可用
- SQLite 是否可写

收益：

- 一眼判断为什么不能运行
- 适合演示前自检
- 降低部署和答辩现场风险

### 10. 改善配置和启动体验

现在项目有 `requirements.txt`、`requirements.semantic.txt` 和 `scripts/start.ps1`。可以进一步降低首次运行成本。

建议：

- 提供 `.env.example`
- README 增加“最小运行模式”和“完整语义检索模式”
- 启动脚本检查虚拟环境和依赖
- 模型未配置时明确说明会进入本地兜底模式
- 对 Windows 用户单独写一段 OCR / Tesseract 配置说明

收益：

- 别人更容易跑起来
- 项目交付感更强
- 简历或答辩分享时减少环境问题

### 11. 加入基础可观测性

`agent/telemetry.py` 已经说明项目有日志意识。可以继续扩展成轻量的本地可观测能力。

建议记录：

- 每次请求耗时
- 是否使用 RAG
- 检索命中数量
- 模型调用是否成功
- 错误类型统计
- 上传解析耗时
- 索引重建耗时

注意：

- 不记录 API Key
- 不默认记录完整用户隐私文本
- 调试日志仅本地开发模式开启

收益：

- 更容易定位慢在哪里
- 答辩时能讲“稳定性设计”
- 后续优化有数据依据

### 12. 做一组标准演示用例

建议准备固定测试问题，覆盖不同能力。

示例：

- “用初学者能懂的话解释 BFS”
- “动态规划为什么需要状态转移？”
- “这段代码为什么会超时？”
- “根据我上传的资料总结前缀和”
- “继续刚才的问题，给一个更简单的例子”
- “不要用知识库，直接讲通俗版”

收益：

- 演示更稳定
- 方便比较改动前后效果
- README 和答辩 PPT 可以直接复用

## 代码层面的具体改进点

### 后端

- 把 `agent/chain.py` 中的模型消息构造、RAG prompt 构造、错误兜底拆得更清楚
- 给聊天请求增加可选参数，例如 `knowledge_base_id`、`use_rag`、`debug`
- 给流式输出增加统一事件类型，例如 `start`、`delta`、`sources`、`error`、`done`
- 上传接口补充更明确的错误码和错误类型
- CORS 在生产环境不要默认 `allow_origins=["*"]`

### 检索

- 为 chunk 增加主题、标题、来源位置和质量评分
- 相邻 chunk 可合并，避免上下文太碎
- 检索结果去重逻辑可以考虑按文档和位置，而不只是 source + text
- fallback 关键词检索可以加入更清晰的分数归一化

### 知识库

- `knowledge_store.json` 可以考虑逐步迁移到 SQLite，避免 JSON 文件在并发写入时风险较高
- 索引构建失败时保留旧索引，不要让知识库进入半可用状态
- 上传草稿定期清理，避免 `data/knowledge_drafts` 积累
- 文档编辑后明确触发索引重建并返回重建状态

### 前端

- 把 `static/script.js` 拆成几个模块，例如聊天、会话、知识库、弹窗、来源面板
- 长文件继续单文件维护会越来越难
- 增加空状态、加载状态、错误状态的一致样式
- 来源面板可以支持点击定位到对应回答
- 输入区增加“当前知识库范围”和“是否启用 RAG”的显式状态

### 测试

- 保留现有 unittest 也可以，但建议逐步迁移或补充 pytest 风格用例
- 增加 FastAPI TestClient 接口测试
- 对 PDF / OCR 这类依赖环境的能力使用 mock，避免测试脆弱
- 对 prompt 构造和上下文裁剪做纯函数测试

## 建议路线图

### 第一阶段：稳定回答质量

1. 重构 prompt 和上下文组装。
2. 强化系统提示词。
3. 增加 RAG 置信度判断。
4. 补 `/api/chat` 和 `/api/chat/stream` 测试。

### 第二阶段：完善学习助手体验

1. 会话摘要升级为学习状态。
2. 前端展示当前知识库范围和本轮来源。
3. 知识库支持按库选择检索范围。
4. 上传预览展示解析质量和 OCR 状态。

### 第三阶段：提升工程和演示可信度

1. 增加诊断页面或诊断接口。
2. 增加 `.env.example` 和更清晰启动说明。
3. 增加本地 telemetry 统计。
4. 准备标准演示问题和截图。

## 最推荐优先做的 5 件事

1. 重构上下文和 prompt 组装，让回答质量先稳定。
2. 给 RAG 检索加低置信判断，避免乱引用。
3. 增加聊天接口和流式接口测试。
4. 上传预览明确显示 PDF / OCR 解析状态。
5. 增加一个运行诊断接口，方便演示前检查环境。

这 5 件事改完后，项目会从“功能已经不少”提升到“体验稳定、逻辑可信、容易展示”的状态。

# 上下文改进建议

本文档记录 AlgoGuide Agent 当前可以优先改进的上下文能力。这里的“上下文”不只是聊天历史，还包括用户问题、知识库检索片段、会话摘要、引用来源、模型提示词和前端展示给用户的依据。

## 当前可以改进什么

### 1. 明确上下文输入结构

当前回答链路已经包含用户问题、知识检索结果和会话历史，但这些信息最好在进入模型前整理成稳定结构，避免模型把不同来源的信息混在一起。

建议把最终 prompt 拆成几个固定区块：

- 用户当前问题
- 最近几轮对话
- 会话摘要
- 检索到的知识片段
- 回答要求
- 不确定性处理规则

这样做的好处是后续更容易调试：如果回答跑偏，可以直接判断是历史干扰、检索片段不准，还是提示词约束不够清楚。

### 2. 给会话摘要增加“学习状态”

现在项目已经有长会话摘要能力，但摘要可以更贴近算法学习场景。普通摘要只记录“聊过什么”，学习状态应该记录“用户掌握到哪里、哪里还卡住”。

建议摘要包含：

- 用户正在学习的主题，例如动态规划、图搜索、前缀和
- 已经解释过的概念
- 用户仍然困惑的点
- 上一次推荐的下一步
- 用户偏好的讲解方式，例如更喜欢代码、图解、例子或推导

这样后续追问时，助手可以更像一个连续辅导的老师，而不是每次从零开始。

### 3. 降低历史对话对当前问题的干扰

聊天历史越长，越容易出现一个问题：模型把旧主题带进新问题里。算法学习尤其明显，例如用户刚问完 BFS，又开始问动态规划，旧上下文可能会污染新回答。

建议加入“主题切换判断”：

- 如果当前问题和上一轮主题相似，保留更多历史
- 如果当前问题明显换题，只保留用户偏好和学习状态
- 如果当前问题包含新的题目描述，优先相信当前题目

这可以让上下文更稳，不会因为历史太多而变钝。

### 4. 优化知识库检索片段

目前项目支持语义检索和来源引用，这是很好的基础。下一步可以改进检索片段的质量，而不只是增加 Top K 数量。

建议优先做：

- 给每个知识片段增加标题、主题标签和难度标签
- 检索结果按主题相关性和片段完整度排序
- 避免把过短、断句严重或公式残缺的片段直接塞进 prompt
- 对同一文档的相邻片段做合并，减少上下文碎片感
- 在没有高置信命中时，明确告诉模型“知识库依据不足”

检索上下文的目标不是越多越好，而是少而准、完整且可引用。

### 5. 让引用来源更可解释

现在回答会展示来源和命中片段，但用户还可以看到更多“为什么引用它”。这会让项目演示时更像一个可靠的 RAG 系统。

建议在引用里展示：

- 来源文件名
- 命中的章节或标题
- 命中原因，例如“包含状态转移定义”或“解释 BFS 使用队列”
- 相关度分数
- 片段摘要

前端不需要展示太多技术细节，但应该让用户能判断答案有没有依据。

### 6. 强化系统提示词

当前 `agent/prompt.py` 的提示词比较简洁，优点是清爽，缺点是对上下文使用规则约束还不够细。

建议补充几条规则：

- 优先回答用户当前问题，不要过度展开旧上下文
- 如果知识片段和用户问题冲突，说明冲突并以用户当前问题为准
- 如果知识库没有依据，不要假装引用
- 算法题优先讲核心思路，再讲状态、转移、边界、复杂度
- 对初学者问题减少术语，对进阶问题给出推导

这类规则能直接改善回答一致性。

### 7. 在前端显示“本轮使用了哪些上下文”

上下文能力如果只在后端发生，用户很难感知。建议在回答下方增加一个轻量的上下文面板。

可以显示：

- 是否使用了知识库
- 使用了几条来源
- 是否使用了会话摘要
- 是否发生了主题切换
- 当前回答依据不足时的提示

这个面板不用复杂，但能显著提升可信度，也方便调试。

### 8. 加入上下文调试日志

为了方便后续继续优化，建议在开发模式下保存每次请求的上下文快照。

快照可以包括：

- 用户问题
- 选中的历史消息
- 会话摘要
- 检索结果列表
- 最终送入模型的 prompt
- 模型是否成功调用
- 是否走了本地兜底回答

注意不要默认记录 API Key 或用户敏感信息。这个能力主要用于本地调试。

## 推荐优先级

### P0：马上值得做

1. 把进入模型的上下文整理成固定区块。
2. 强化 `agent/prompt.py` 的上下文使用规则。
3. 给检索结果增加“知识库依据不足”的判断。

这三项改动范围小，但会直接提升回答稳定性。

### P1：下一轮做

1. 会话摘要增加学习状态。
2. 判断当前问题是否发生主题切换。
3. 前端展示本轮使用的上下文来源。

这三项会让项目从普通聊天助手更像算法学习助手。

### P2：演示和答辩加分项

1. 保存上下文调试快照。
2. 引用来源展示命中原因。
3. 检索片段按标题、主题、难度进行组织。

这些功能对演示很有帮助，能体现项目不是简单套壳，而是在认真处理 RAG 的可解释性和连续对话问题。

## 建议实现顺序

1. 先改后端 prompt 组装逻辑，让上下文结构稳定。
2. 再改系统提示词，明确模型如何使用上下文。
3. 然后改检索结果过滤，减少低质量片段进入 prompt。
4. 接着扩展会话摘要，让它记录用户学习状态。
5. 最后在前端展示上下文使用情况。

## 一个推荐的上下文模板

```text
你是 AlgoGuide Agent，一个算法学习助手。

[当前问题]
{user_question}

[会话摘要]
{session_summary}

[最近对话]
{recent_messages}

[知识库片段]
{retrieved_context}

[回答要求]
- 优先回答当前问题。
- 如果知识库片段相关，就自然引用。
- 如果知识库依据不足，直接说明。
- 先讲核心思路，再讲步骤、代码或复杂度。
- 不要把旧主题强行带入新问题。
```

## 判断改进是否有效

可以用下面几个问题测试：

1. 用户连续追问同一道题时，助手是否能记住前面的解释？
2. 用户突然换题时，助手是否不会被旧上下文带偏？
3. 知识库没有相关内容时，助手是否会诚实说明依据不足？
4. 回答里的引用是否真的对应知识片段？
5. 用户刷新页面后继续问，助手是否还能接住学习进度？

如果这几个问题表现稳定，说明上下文能力已经比普通聊天 Demo 强很多。

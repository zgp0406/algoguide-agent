{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run AlgoGuide Agent",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app:app", "--reload"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
